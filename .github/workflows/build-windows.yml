name: Build scrcpy.exe

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: 检查系统环境
        run: |
          echo "Windows版本：$env:OS"
          gcc --version

      - name: 安装依赖工具
        run: |
          # 安装Chocolatey
          Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
          
          # 安装编译工具
          choco install -y meson ninja mingw
          choco install -y ffmpeg-full --version=5.1.2

      - name: 手动安装libusb
        run: |
          # 创建临时目录
          mkdir C:\libusb
          cd C:\libusb
          
          # 从GitHub下载libusb预编译库
          Invoke-WebRequest -Uri "https://github.com/libusb/libusb/releases/download/v1.0.26/libusb-1.0.26.7z" -OutFile "libusb.7z"
          
          # 解压7z文件（需要安装7-Zip）
          choco install -y 7zip
          & 'C:\Program Files\7-Zip\7z.exe' x "libusb.7z" -oC:\libusb
          
          # 设置环境变量，让meson找到libusb
          echo "C:\libusb\libusb-1.0.26\MinGW64\dll" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "C:\libusb\libusb-1.0.26\MinGW64\include" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: 拉取scrcpy源码
        run: |
          git clone https://github.com/Genymobile/scrcpy.git
          cd scrcpy
          git checkout v2.4  # 可替换为最新版本

      - name: 配置编译选项（仅客户端）
        run: |
          cd scrcpy
          meson setup build --buildtype=release `
            -Dprebuilt_server=false `
            -Dbuild_server=false `
            -Dffmpeg=system `
            -Dlibusb=enabled  # 启用libusb

      - name: 编译scrcpy.exe
        run: |
          cd scrcpy
          ninja -C build

      - name: 上传scrcpy.exe作为产物
        uses: actions/upload-artifact@v4
        with:
          name: scrcpy-exe
          path: |
            scrcpy/build/src/scrcpy.exe
            C:\libusb\libusb-1.0.26\MinGW64\dll\libusb-1.0.dll  # 同时上传依赖的DLL
